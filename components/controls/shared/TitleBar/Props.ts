export interface Props {
    /**
     * @supported Windows
     */
    extendIntoView?: boolean;

    /**
     * @supported Windows
     */
    disableAutoBackButton?: boolean;

    /**
     * @supported Windows
     */
    transparent?: boolean;
}
