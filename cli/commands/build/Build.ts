import { NitrexAppConfig } from "../../NitrexAppConfig";
import { BuildFlags } from "./BuildFlags";

export class Build {
    public constructor(
        args: string[],
        flags: BuildFlags,
        config: NitrexAppConfig
    ) {
    }
}
